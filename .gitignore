node_modules/
API KEY OpenRouter.txt
API TELEGRAM.txt
API Keys _ Settings _ OpenRouter.html
API Keys _ Settings _ OpenRouter_files/
*.env
.env*

# Environment & Secrets (AIOX)
.env
.env.local
.env.*.local
*.key
*.pem

# Dependencies (AIOX)
node_modules

# Build & Logs (AIOX)
dist/
build/
*.log
logs/

# IDE & OS (AIOX)
.DS_Store
Thumbs.db
.idea/
*.swp

# AIOX Local (AIOX)
.aiox-core/local/
.claude/settings.local.json
.aiox/install-log.txt

# Holding de Robôs — serviços Python (extraídos pra fora do Kernel Workspace em 2026-08-10,
# entradas mantidas por precaução caso algum .db solto ainda apareça por engano)
__pycache__/
*.pyc

# Repos aninhados com .git próprio, extraídos pra pasta irmã (ver PLANO-reorganizacao-workspace.md)
# — nunca devem voltar a ser trackeados aqui, cada um já tem seu próprio remote/histórico.
sistema-thieco/
kernel/
vilamill-sistema/
lane-confeitaria/
lanchonete-sistema/
kernelmei/
kernel-foodservice/
Kernel-Kalel/
Kernel-brainiac/
cortex/
quasar/
kernel-academia/
evolution-api/
